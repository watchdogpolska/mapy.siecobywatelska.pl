{% extends 'base.html.twig' %}

{% block body %}
    <div class="pull-right">
        <a href="{{ path('attachment_index') }}" class="btn btn-default">Back to the list</a>
        <a href="{{ path('point_show', {'id': point.id}) }}" class="btn btn-default">Back to the point</a>
    </div>
    <h1>Attachment creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
{% endblock %}
